<%= render "components/jumbottron" %>

<ul>
  <% @restaurants.each do |r| %>
  <li>
  <%= cl_image_tag r.photo, height: 200, width: 300, crop: :fill %>
    <h2><%= r.name %></h2>
    <p><%= r.category %></p>
    <p><%= r.address %></p>
    <p><%= r.address2 %></p>

  </li>
  <% end %>
</ul>
